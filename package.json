{
  "name": "redis-service",
  "version": "1.2.0",
  "description": "redis common service",
  "main": "dist/index.js",
  "types": "types",
  "scripts": {
    "build": "rm -rf dist types && tsc",
    "pub": "git push --all && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/isLishude/redis-service"
  },
  "author": "lishude",
  "license": "MIT",
  "devDependencies": {
    "@types/ioredis": "^3.2.8",
    "@types/node": "^10.1.0",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.12.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "ioredis": "^3.2.2"
  }
}
